# Frontend refinement QA

## Live UI audit of the deployed app - 2026-09-11

Measured against `https://splitbind.qivarn.id.vn` in a real Chrome session at `innerWidth` 1680, authenticated. These are current defects, not historical notes.

- **The route stagger timeline has no targets.** `MotionRoute.tsx` builds a GSAP stagger over `stage.querySelectorAll("[data-motion-block]")`, but `data-motion-block` is present on exactly one element type in the source: the `.history-card` article in `HistoryPage.tsx`. A live `document.querySelectorAll('[data-motion-block]').length` returned `0` on `/issue` and on `/issuances/:id`. Every page except `/history` therefore receives only the single 0.26s whole-stage opacity fade, which is imperceptible on a page that is still fetching. Any claim that the app "has GSAP animation" must name the page: outside `/history` the second half of the timeline is dead code.
- **`.result-page` is left-aligned inside a wider stage.** `app.css` declares `.result-page { max-width: 880px; }` with no `margin-inline: auto`. At 1680px the shell measures header and nav at 1120px (centred, 273 to 1393) while `.route-stage` is 1056px (305 to 1361) and the result body is 880px starting at 305, so roughly 176px of empty space sits to the right of every verdict, issuance record and job record while the rules above them run the full width. This reads as a broken layout rather than a narrow reading measure.
- **The stylesheet carries almost no motion.** `app.css` is 1165 lines with zero `@keyframes` and four `transition` declarations, one of which is the `prefers-reduced-motion` kill switch; eight `:hover` rules exist in total. The only `@keyframes` resolvable in the live document was `claude-pulse`, injected by the browser extension, not by the app. Hover, focus and press states are therefore instant steps with no easing, which is the measurable part of the "feels unfinished" complaint.
- **Session gate flashes an unstyled line.** A hard navigation to `/verify` or `/history` renders `SessionBoundary`'s `<main class="session-loading"><p>Đang kiểm tra phiên đăng nhập</p></main>` with no nav, no skeleton and no brand context, then swaps to the full page. Screenshots taken within roughly 2 seconds of navigation captured this state on both routes.
- **`resize_window` could not exercise the breakpoints in this session**, so the responsive behaviour recorded on 2026-09-10 was not re-verified here. See `agent-tools-and-plugins.md`.

## UX, ETA, Full Identifiers & History Architecture - 2026-09-10

- **Dynamic ETA and Elapsed Timer Invariant:** On `JobDetailPage.tsx`, static wait notes ("Thời gian xử lý phụ thuộc kích thước tệp và hàng đợi. Chưa có ước tính thời gian còn lại.") are replaced with an active elapsed timer. For jobs in non-terminal states (`created`, `queued`, `processing`, `retryable_failed`):
  - $\le 15$ seconds: displays `Thời gian ước tính: ~5 - 15 giây · Đã xử lý: {elapsed}s`.
  - $> 15$ seconds: dynamically transitions to queue status `Đang xử lý trong hàng đợi ({elapsed}s) · Vui lòng giữ nguyên trang...`.
  - Upon completion (`succeeded`): displays `Đã hoàn tất sau {totalSeconds} giây.` (computed from `data.created_at` to `data.updated_at`), providing immediate quantitative confirmation.
- **Full UUID Display Invariant:** Component `CompactIdentifier` supports a boolean `full` prop. On record and detail pages (`/jobs/:id`, `/issuances/:id`, `/verifications/:id`), `full={true}` renders complete 36-character UUID strings in monospace font with a copy button. Defensive styling `overflow-wrap: anywhere; word-break: break-all;` on `<code>` prevents horizontal viewport overflow on narrow screens.
- **Multi-Tenant Scoped History & Navigation:** Backend endpoint `GET /api/v1/jobs` combined with `scope_jobs(actor, queryset).select_related("issuance__recipient", "verification")` provides an isolated, performant document and job history endpoint (Admin/Auditor sees organization-wide jobs; Issuer sees own issuances; Verifier sees own verifications). The frontend `/history` route and `History` navigation tab provide instantaneous lookup and filtering (All / Issuance / Verification) with direct links to `/issuances/:id` and `/verifications/:id`.
- **Strict Design System Token Boundary:** `apps/web/src/test/design-system.test.tsx` strictly validates that all CSS `var(--token)` usages in `app.css` match keys declared in `:root` inside `tokens.css`. CSS fallback syntax like `var(--radius-pill, 9999px)` fails this regex validation if `--radius-pill` is not explicitly declared. All CSS rules must strictly use declared tokens (`--radius-input`, `--color-surface`, etc.) or standard CSS literals.

## Production evidence correction - 2026-09-09

Accepted live browser evidence on September 10: login, synthetic issuance, processing, actual artifact download, unchanged-file verification (`VERIFIED_INTACT`, hash true, signature true), byte-modified verification (`NO_WATERMARK`, hash false, signature null), keyboard evidence disclosure and logout passed with no console/page errors. Both verdicts passed at 1920x1080, 1280x800, 768x1024 and 375x667; mobile used reduced motion and all widths passed overflow assertions. Nine live PNGs remain in `artifacts/live-integrity-report`; 34 E2E PNGs were moved to `artifacts/evidence-report-71270c6`. Visual inspection confirmed clear verdict-first hierarchy and stacked mobile evidence fields. Final full Chromium suite passed 20/20; September 10 rerun passed 82/82 unit/component tests and standalone typecheck. Release CI verified the production build. Lint remains unconfigured. Disposable build/digest outputs and Playwright run metadata were removed; no Node processes remained. Synthetic records and server rollback evidence are intentionally retained.

September 10 continuation: the first live-smoke invocation stopped before browser launch because its environment variable mistyped the executable as `chromium.exe`. Directory inspection verified `chrome.exe` in the existing Chromium installation; correct only the invocation, without reinstalling browsers or changing production. This was a command transcription error, not an application failure.

Commit `71270c6` implements the correction, PR #4. Final pre-publication checks passed: 82 unit/component tests; full 19-case Chromium suite followed by 7-case integrity suite with the additional missing-manifest scenario; production build/typecheck and diff-check. Independent read-only review found no Critical/Important issues. The live smoke script is durable source and requires explicit SMOKE_ALLOW_WRITES=1 plus HTTPS origin and externally supplied credentials/recipient; it creates synthetic records only and does not log credentials or signed download URLs. Production rollout and authenticated live results remain a separate gate.

The first full suite found three obsolete exact-copy assertions tied to the former mismatch/manifest text; update those assertions while retaining no-watermark-claim and error-precedence behavior. Six fresh Chromium scenarios passed across intact, unmatched, incomplete, ambiguous, invalid and processing-failed results, with keyboard disclosure, reduced motion and responsive overflow checks. The shared shell must suppress both integrity and demo notes on result routes, not fall through its conditional into a misleading demo banner.

The first fresh regression run reproduced five evidence-presentation failures (plus two existing error-precedence passes); the corrected integrity renderer passed all seven. TypeScript then caught a third argument accidentally attached to the obsolete two-argument mismatch helper. Remove that obsolete branch from the research renderer now that integrity mode dispatches separately; do not widen the helper signature to conceal the integration error.

After a user continuation, the prior test process ID was no longer available (write_stdin: Unknown process id). Do not infer its result; rerun the bounded suite and observe fresh output. No source change was lost.

The user rejected the evidence panel's placeholder metrics and requested a production-quality correction, not cosmetic hiding. Source trace confirms the integrity worker resolves exact SHA-256 matches only within the current organization. No exact candidate returns NO_WATERMARK, exact_file_hash_match=false and manifest_signature_valid=null: this means no matching issuance was located, not proof of editing or a failed signature. The serializer carries legacy zero fingerprint/vote fields even in integrity mode. Presentation must explicitly branch on algorithm_label, distinguish unknown from not evaluated, exclude unsupported research measurements from integrity reports, and avoid implying a specific original was compared when none was identified. Existing API/security contracts must remain intact.

## Refactor checks

Final process cleanup verified zero task-matching Node processes after intentionally interrupting the Vite server (the interrupted session returned exit 1 with no error output). Requested motion-settled and reduced-motion PNGs remain present; duplicate temporary outputs and unusable recordings were removed. No worktree or user source was discarded.

Final observed checks: 72/72 unit/component tests with one worker, 13/13 Chromium E2E (including real route-opacity sampling and reduced-motion), typecheck, production build and diff-check passed. Six-viewport fixture walkthrough passed; final 375px re-capture passed after normalizing scroll. Web lint is not configured. Live backend cryptography, production deployment, other browser engines and prolonged soak tests remain unverified. The native exact-target cleanup documented in workspace lifecycle succeeded after shell rejection: removed only task-generated duplicate E2E outputs and unusable video/contact-sheet artifacts, preserving before/after screenshots and live-motion screenshots. The app changes remain uncommitted.

Cleanup targets were resolved and checked for reparse points. The shell tool rejected the combined Copy-Item/Remove-Item command before execution, so none of those operations is claimed complete. Use the documented exact-target cleanup mechanism only within the verified task artifact paths; keep requested before/after screenshots.

The constrained rerun `npm test -- --maxWorkers=1` passed all 72 tests without unhandled errors; a direct Node child-process smoke check also passed. No persistent application defect was established for the prior concurrent worker-start timeouts. Video frame extraction remained blank despite frame-readiness checks, so the inspection helper and blank contact sheet are not accepted as visual evidence. Use live browser style sampling for the route tween and reduced-motion assertion; retain screenshots as primary visual evidence.

After an interrupted session, a fresh default Vitest run failed to start seven fork workers (worker-response timeouts, not assertion failures). The E2E suite still passed 12/12. Stop the task-owned video inspection browser and retry with the locally documented `--maxWorkers=1`; do not ignore unhandled errors. A diagnostic read accidentally duplicated `apps/web` relative to an already-web cwd; resolve paths from the actual cwd. Initial video contact-sheet extraction showed blank early frames; decoding inspection must wait for actual frames, bound callbacks and release the browser even when sampling fails.

Full-page screenshots taken after auto-scroll can include the offscreen fixed skip link at its document-relative location even though it is outside the visible viewport. Reset scroll to the document start before full-page capture; do not remove the keyboard skip link to fix a screenshot artifact. Browser stress checks now include native picker, long filename, retained file after upload failure, retry, persistent processing, logout/login error, PDF download and reduced-motion rendering. Palette contrast checks use canvas-resolved browser colors and WCAG luminance ratios.

After corrections, all 12 existing Chromium E2E tests and all 72 unit/component tests passed. Visual review at 1280x800 still showed the initial issuance CTA below the fold despite clean overflow checks. Replace the desktop 236px stacked file picker with a 160px horizontal picker and reduce heading-to-form spacing; mobile retains the vertical picker. Screenshot review must assess task reachability, not only document overflow.

Concurrent browser and unit suites exposed the default 1-second Testing Library wait in the chained login-to-role-home redirect test: the snapshot was the intermediate home route, not access denial. The same 8-test file passed unchanged in isolation (486ms total test execution). Give the asynchronous redirect assertion a 5-second ceiling, retaining its target route and heading assertions. Run final checks separately so a shell's last successful command cannot mask a prior test failure.

The initial fresh E2E run had 6 failures and 6 passes. In addition to collapsed disclosure, two workflow sequence assertions incorrectly counted the unrelated capabilities GET as an upload step. Exclude session/capabilities queries from the workflow trace while preserving the exact upload, finalize, create and job sequence. The evidence headings changed; verify current technical-data and limitations sections plus existing factual cautions instead of obsolete four-panel titles.

Final browser regression investigation: three authorization tests read limitations inside the newly collapsed technical disclosure without opening it. Keep progressive disclosure and make tests activate its summary before asserting visibility. The authorization fixture also omitted `/demo/capabilities`, causing Vite to contact the absent local backend; explicitly fixture that endpoint. This does not indicate a production failure.

Review caught that successful issuance and downloadable artifact availability are separate API fields. A successful issuance without an available PDF must not claim its PDF is ready; a regression test now covers this. Login returns through the role-aware home route instead of assuming every role can issue. Removed the unused cryptographic animation component after reference search. Reviewer guessed two nonexistent paths before locating the actual session.tsx and splitbind service tree; no source changes resulted from those read errors.

Fresh full frontend checks after updating behavior-focused tests: 71 tests passed; TypeScript passed. Additional regression showed PROCESSING_FAILED/INVALID_MANIFEST must take precedence over a false exact-hash field; otherwise a processing error is mislabeled a mismatch. This precedence now passes its regression tests.

Pass-1 screenshots cover all four requested viewports with no page errors. Mobile verdict prose was too narrow beside a 48px mark; stack the mark above text on small viewports. Remove repeated conclusions from the DOM via optional disclosure composition, not display:none. Desktop heading-to-form spacing was tightened.

An attempted patch listed CSS hunks out of file order and was rejected; reordering hunks resolved it. One test runner was silent while the session was interrupted; by the time cleanup ran its inspected process IDs had exited (NoProcessFound). A fresh verbose run completed normally; no application root cause is asserted. The independent reviewer initially hit account quota; resumed once after the quota window.

Five focused regression tests now pass after reproducing the non-intact false-accusation bug and dropped-file native validation bug. File input `required` must depend on selected application state because a dropped file is not automatically installed into the native input's FileList. Keep the upload validator as the final authority.

Removing GSAP imports while leaving an unused `useRef` caused a test-time ReferenceError; remove both the effect and its ref. Exact patches against BOM-prefixed TSX first lines failed: preserve BOM when constructing the old-text patch (Node readFileSync with JSON transport), or avoid first-line anchors. A composed tool script also had an unmatched closing brace; no actions executed until syntax was corrected. apply_patch rejects delete+add for the same path in one patch; execute separate validated patch operations when replacing a stylesheet. A deliberately incomplete knowledge patch anchor was rejected without writing; use exact current lines.

The first full suite after refactor has nine failures: eight obsolete visual/copy assertions tied to removed chambers/seals/breadcrumbs, and one duplicate conclusion caused by leaving hidden evidence markup. Replace design-change detectors with outcome assertions; remove duplicate DOM rather than hiding it with CSS. Network/validation tests remain the contract.

## Scope and evidence - 2026-09-08

The user authorizes an autonomous presentation-layer rebuild, not preservation of prior motion. API, authentication, validation and security boundaries remain unchanged. Work in `.worktrees/splitbind-mvp`; portable knowledge resides in the parent project's `docs/knowledge`, not in that worktree (its knowledge index does not exist).

Baseline Chromium inspection shows a narrow form column inside another two-column panel, multiple companion cards, and five decorative processing stages. Source inspection confirms upload intent/uploading are mislabeled as DWT decomposition; those browser stages do not attest that server processing occurred. Verification treats every non-intact outcome as tampering and displays positive signature language for unknown values. Replace this with verdict-specific, nullable-evidence-aware presentation.

QA uses the real Vite/React application with explicit API response fixtures, not production writes. Chromium executable was verified under the installed `chromium-1234/chrome-win64` directory. Screenshot consumption must wait for the producer to finish that viewport; reading an anticipated file before creation yields a missing-file tool error.

The initial fixture glob `**/api/**` accidentally intercepted Vite's `/src/api/client.ts` module and produced a blank page. Narrowing it to `**/api/v1/**` restored login and issuance walkthroughs. Fixture routes must target API namespaces, not source-directory names. This is QA tooling scope, not an application authentication defect.
